# ---------------------------------------------------------------------------
# Line endings policy
# ---------------------------------------------------------------------------
# Default: detect text and normalize (unless overridden below)
* text=auto

# Don't let JSON get rewritten (project requirement)
*.json -text binary diff=astextplain

# ---------------------------------------------------------------------------
# Documents / text formats
# ---------------------------------------------------------------------------
*.bibtex   text diff=bibtex
*.doc      -text diff=astextplain
*.DOC      -text diff=astextplain
*.docx     -text diff=astextplain
*.DOCX     -text diff=astextplain
*.dot      -text diff=astextplain
*.DOT      -text diff=astextplain
*.pdf      -text diff=astextplain
*.PDF      -text diff=astextplain
*.rtf      -text diff=astextplain
*.RTF      -text diff=astextplain

# Keep markdown/text files consistent across platforms (avoid CRLF noise)
*.md       text eol=lf
*.tex      text eol=lf diff=tex
*.adoc     text eol=lf
*.textile  text eol=lf
*.mustache text eol=lf
*.csv      text eol=lf
*.tab      text eol=lf
*.tsv      text eol=lf
*.txt      text eol=lf
*.sql      text eol=lf

# ---------------------------------------------------------------------------
# Config / serialization (LF to avoid CI diffs)
# ---------------------------------------------------------------------------
*.toml     text eol=lf
*.xml      text eol=lf
*.yaml     text eol=lf
*.yml      text eol=lf

# Patch files should be LF (prevents accidental patch corruption)
*.patch    text eol=lf
*.diff     text eol=lf

# ---------------------------------------------------------------------------
# Graphics / assets
# ---------------------------------------------------------------------------
*.png      -text binary
*.jpg      -text binary
*.jpeg     -text binary
*.gif      -text binary
*.tif      -text binary
*.tiff     -text binary
*.ico      -text binary
# SVG: keep as text unless you explicitly want it binary
*.svg      text eol=lf
*.eps      -text binary

# ---------------------------------------------------------------------------
# Scripts
# ---------------------------------------------------------------------------
# POSIX scripts must be LF
*.bash     text eol=lf
*.sh       text eol=lf

# Windows scripts should be CRLF
*.bat      text eol=crlf
*.cmd      text eol=crlf
*.ps1      text eol=crlf

# ---------------------------------------------------------------------------
# Sources (force LF so diffs are stable across platforms)
# ---------------------------------------------------------------------------
*.c     text eol=lf diff=c
*.cc    text eol=lf diff=cpp
*.cxx   text eol=lf diff=cpp
*.cpp   text eol=lf diff=cpp
*.c++   text eol=lf diff=cpp
*.h     text eol=lf diff=c
*.hpp   text eol=lf diff=cpp
*.h++   text eol=lf diff=cpp
*.hh    text eol=lf diff=cpp

# ---------------------------------------------------------------------------
# Build / binary outputs
# ---------------------------------------------------------------------------
*.7z       -text binary
*.gz       -text binary
*.tar      -text binary
*.zip      -text binary

*.slo      -text binary
*.lo       -text binary
*.o        -text binary
*.obj      -text binary

*.gch      -text binary
*.pch      -text binary

*.so       -text binary
*.dylib    -text binary
*.dll      -text binary

*.lai      -text binary
*.la       -text binary
*.a        -text binary
*.lib      -text binary

*.exe      -text binary
*.out      -text binary
*.app      -text binary

# ---------------------------------------------------------------------------
# Exclude from `git archive` exports
# ---------------------------------------------------------------------------
.gitattributes export-ignore
.gitignore     export-ignore
.editorconfig  export-ignore
